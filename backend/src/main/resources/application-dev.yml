spring:
  graphql:
    graphiql:
      enabled: true   # Enable GraphiQL playground in dev

  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.secureleaf: DEBUG
    org.springframework.security: DEBUG
    org.springframework.graphql: DEBUG
